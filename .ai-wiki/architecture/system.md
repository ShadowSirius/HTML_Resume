# architecture.system

`rs_portrait.html::DOMContentLoaded` → generated unpacker → embedded `Component` → `Component.buildViz` → SVG/foreignObject portrait tree.

Durable state is localStorage-backed and exported by `Component.exportResume`. Source data remains embedded in the packed page.
