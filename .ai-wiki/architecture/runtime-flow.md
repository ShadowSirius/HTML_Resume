# architecture.runtime-flow

`rs_portrait.html::DOMContentLoaded` → template/manifest unpack → `Component` construction → localStorage migration/load → `Component.buildViz` → pointer handlers and editor actions → localStorage persistence / `Component.exportResume` download.
